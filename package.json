{
  "name": "desafio-agendamento-petshop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch server.json --port 3333F"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
