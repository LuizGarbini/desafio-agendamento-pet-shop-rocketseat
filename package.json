{
  "name": "desafio-agendamento-petshop",
  "description": "Projeto Web de agendamento de consulta no petshop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch server.json --port 3333F"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
